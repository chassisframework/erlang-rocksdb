name: build 

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

permissions:
  contents: read

env:
  ERLANG_ROCKSDB_OPTS: "-DWITH_BUNDLE_LZ4=ON -DWITH_BUNDLE_SNAPPY=ON"

jobs:
  linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        otp:  ["25.3", "26.2", "27.2"]
        rebar3: ['3.24.0']
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: erlef/setup-beam@v1
        with:
          otp-version: ${{matrix.otp}}
          rebar3-version: ${{matrix.rebar3}}
      - uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: cmake
          version: 1.1
      - name: build
        run: rebar3 eunit
  linux-shared-libs:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        otp:  ["25.3", "26.2", "27.2"]
        rebar3: ['3.24.0']
    env:
      # Override global env to use system libraries instead of bundled
      ERLANG_ROCKSDB_OPTS: "-DWITH_LZ4=ON -DWITH_SNAPPY=ON"
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: erlef/setup-beam@v1
        with:
          otp-version: ${{matrix.otp}}
          rebar3-version: ${{matrix.rebar3}}
      - uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: cmake liblz4-dev libsnappy-dev
          version: 1.1
      - name: build
        run: rebar3 eunit
  macos:
    runs-on: macos-26
    strategy:
      matrix:
        otp:  ["27"]
        rebar3: ['3.24.0']
    steps:
      - name: prepare
        env:
          HOMEBREW_NO_INSTALLED_DEPENDENTS_CHECK: "1"
        run: |
          brew update
          brew install erlang@${{ matrix.otp }}
            echo "$(brew --prefix erlang@${{ matrix.otp }})/bin" >> $GITHUB_PATH
      - name: install rebar3
        run: |
          wget https://github.com/erlang/rebar3/releases/download/${{ matrix.rebar3 }}/rebar3 && chmod +x rebar3
          sudo mv rebar3 /usr/local/bin/ && sudo chmod +x /usr/local/bin/rebar3
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: build
        run: make
  freebsd:
    runs-on: ubuntu-latest
    env:
      ERLANG_ROCKSDB_OPTS: "-DWITH_BUNDLE_LZ4=ON -DWITH_BUNDLE_SNAPPY=ON"
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Test on FreeBSD
        uses: vmactions/freebsd-vm@v1
        with:
          release: "14.2"
          usesh: true
          envs: 'ERLANG_ROCKSDB_OPTS'
          prepare: |
            pkg upgrade -y
            pkg install -y erlang-runtime28 rebar3 cmake git gmake llvm18
          run: |
            # Ensure Erlang 28 is in PATH
            export PATH="/usr/local/lib/erlang28/bin:$PATH"
            echo "Erlang version:"
            erl -eval 'erlang:display(erlang:system_info(otp_release)), halt().' -noshell
            # Use LLVM 18 clang which fully supports C++20
            export CC=/usr/local/bin/clang18
            export CXX=/usr/local/bin/clang++18
            echo "Using CC=$CC CXX=$CXX"
            $CXX --version
            git config --global --add safe.directory $GITHUB_WORKSPACE
            rebar3 eunit
